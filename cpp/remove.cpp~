#include <iostream>
using namespace std;

int removeElement(int[] nums, int val);

int main() {
  int[4] nums =  {3, 2, 2, 3};
  int res = removeElement(nums, 3);
  // DEBUG: print result
  cout << "k = " < res << endl;
  for (int i=0; i < 4; i++) {
    cout << nums[i] << endl;
  }
  
  return 0;
}

int removeElement(int[] nums, int val) {
  return val;
}
